<form method="post" class="deleteForm" action="{{ path('app_quiz_c_r_u_d_delete', {'id': quiz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz.id) }}">
    <button class="btn delete">Delete</button>
</form>